version: '3.8'

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
    restart: unless-stopped

  # База данных PostgreSQL (для будущего использования)
  # postgres:
  #   image: postgres:15-alpine
  #   environment:
  #     POSTGRES_USER: bcai
  #     POSTGRES_PASSWORD: bcai_secret
  #     POSTGRES_DB: business_control
  #   volumes:
  #     - postgres_data:/var/lib/postgresql/data
  #   ports:
  #     - "5432:5432"

# volumes:
#   postgres_data:
